{
  "tabs": [
    {
      "id": "general",
      "label": "General",
      "fields": [
        {
          "key": "debug_mode",
          "label": "Debug Mode",
          "component": "checkbox",
          "description": "Enable verbose debug logging in the withdraw user UI"
        }
      ]
    }
  ]
}
